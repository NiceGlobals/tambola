export class Ticket {
  tkey?: string;         // ticket id not in use
  tid?: string;         // ticket id
  gameid?: string;      // game id
  gt?: number;          // game time
  userid?: string;      // user id
  tArr?: any;           // ticket array
  twin?: any;           // ticket win or not
  userName?: string;    // username
  phone?: string;       // user phone
  email?: string;       // user email
  tit?: number;         //ticket registration time
}
